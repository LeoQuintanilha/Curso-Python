
"""
Operadores de comparação (relacionais)

OP      Significado          Exemplo( True )
>       maior                2 > 1
>=      maior ou igual       2 >= 2
<       menor                1 < 2
<=      menor ou igual       2 <= 2
==      igual                'a' == 'a'
!=      diferente            'a' != 'b'

"""

maior = 2 > 1
maior_ou_igual = 2 >= 2
menor = 1 < 2
menor_ou_igual = 2 <= 2
igual = 'a' == 'a'
diferente = 'a' != 'b'

print('Atenção no meu código!!!')

#Nesta aula o professor ensinou a carregar o SHELL do Python. Isto significa que quando carregamos o Shell não precisarei ficar alterando o PRINT e rodar.
#Só vou digitar o valor da variável e só.
#para carregar o SHELL do Python devo digitar a linha de comando (python -i aula19.py) no terminal.
#Depois que eu carregar o SHELL não posso alterar o modulo que estou trabalhando. Se eu alterar o valor do módulo terei que sair e voltar do SHELL. 
#Para sair do SHELL é só digitar EXIT.